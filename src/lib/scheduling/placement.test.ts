import { describe, expect, it } from "vitest";
import { snapMinutes, validatePlacement } from "@/lib/scheduling/placement";
const fixed = [{ startMinutes: 480, endMinutes: 720 }, { startMinutes: 720, endMinutes: 765 }, { startMinutes: 840, endMinutes: 1020 }];
describe("placement validation", () => { it("snaps", () => { expect(snapMinutes(1022)).toBe(1020); expect(snapMinutes(1023)).toBe(1025); }); it("honours fixed boundaries", () => { expect(validatePlacement({ startMinutes: 465, durationMinutes: 15, fixedSchedules: fixed, scheduledTasks: [] }).valid).toBe(true); expect(validatePlacement({ startMinutes: 470, durationMinutes: 15, fixedSchedules: fixed, scheduledTasks: [] }).valid).toBe(false); }); });
describe("reschedule availability", () => { const chinese = { id: "chinese", dailyTaskId: "chinese", taskTemplateId: "chinese", startMinutes: 390, durationMinutes: 30 }; const next = { id: "next", dailyTaskId: "next", taskTemplateId: "next", startMinutes: 440, durationMinutes: 20 }; const check = (startMinutes: number) => validatePlacement({ startMinutes, durationMinutes: 30, fixedSchedules: [], scheduledTasks: [chinese, next], ignoreScheduledTaskId: "chinese" }).valid; it("temporarily removes the moving task", () => { expect(check(395)).toBe(true); expect(check(400)).toBe(true); expect(check(410)).toBe(true); expect(check(415)).toBe(false); }); it("keeps fixed schedules authoritative", () => { expect(validatePlacement({ startMinutes: 470, durationMinutes: 15, fixedSchedules: [{ startMinutes: 480, endMinutes: 720 }], scheduledTasks: [chinese], ignoreScheduledTaskId: "chinese" }).valid).toBe(false); }); });
